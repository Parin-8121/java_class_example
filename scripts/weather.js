class Weather {
  constructor(city, condition) {
    this.city = city;
    this.condition = condition;
  }

  getCity() {
    return this.city;
  }

  getCondition() {
    return this.condition;
  }
}
